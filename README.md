# Linux-mono-audio-.Mov-video-files-to-stereo-converter-script
ffmpeg should be installed first.

replace "/name/your/path" with the path of the video files (tested only with .Mov) you need to convert.

Tested only on Linux.
